import { processMessage } from '$root/processMessage';

describe('processMessage', () => {
  describe('unit: processWith', () => {
    it('should run', () => {});
  });
  describe('integration: processMessage', () => {
    it('should run', () => {});
  });
});
